import JscadModelViewer from "@jscad/components/JscadModelViewer.astro";
import { cm, inch, mm } from "@pocs/monitor-riser-speaker-model/values";
import { MonitorRiserSpeakerModel, materials } from "@pocs/monitor-riser-speaker-model/model";
import SpeakerViewer from "@pocs/3d-speaker-viewer/Viewer";

export const speaker = {
  width: cm(19),
  height: cm(19),
  depth: cm(30),
  enclosureBoardThickness: mm(21),
  driver: {
    tweeter: inch(1.1),
    woofer: inch(4)
  },
  bassPort: inch(1.1),
};

# Speaker model

<JscadModelViewer
  name="monitor-riser-speaker-model"
  model={MonitorRiserSpeakerModel(speaker)}
  materials={materials}
  style={{ width: "100%%", height: "50hv" }}
/>

# Old

<SpeakerViewer
  configuration="Separate Tweeter & Woofer"
  mode="model-only"
  width={mm(speaker.width.value * 10)}
  height={mm(speaker.height.value * 10)}
  depth={mm(speaker.depth.value * 10)}
  client:only="react"
/>